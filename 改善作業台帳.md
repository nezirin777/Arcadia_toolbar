# ArcadiaToolBar Next 改善作業台帳

- 作成日: 2026-09-16
- 基準コミット: `0b5515e` (`test: 公開可能な合成HTMLフィクスチャを追加`)
- 対象: `Arcadia_toolbar_next.js`、`fixtures/`、利用者向け文書
- 基本方針: Userscript単体配布を維持し、回帰確認を先に整備してから小単位で改善する

## 状態の定義

| 状態 | 意味 |
| --- | --- |
| 未着手 | 問題または候補を確認済みだが、変更していない |
| 対応中 | 実装または検証を進めている |
| 確認待ち | 実装済みで、実ブラウザなどの確認が残っている |
| 完了 | 実装と必要な検証が完了している |
| 要判断 | 挙動や構造の選択について利用者判断が必要 |
| 見送り | 理由を記録したうえで対応対象から外した |

## 改善項目

| ID | 優先度 | 状態 | 項目 | 現状・根拠 | 対象 | 完了条件 |
| --- | --- | --- | --- | --- | --- | --- |
| R-01 | P0 | 完了 | 捜索掲示板フィクスチャのURL修正 | リンクとフォームactionを `/bbs/sss/sss.php` へ統一した | `fixtures/search-list.html`、`fixtures/search-article.html` | ブラウザ回帰 T-11、T-12 OK |
| R-02 | P0 | 完了 | メイン掲示板フィクスチャのURL修正 | リンクとフォームactionを `/bbs/mainbbs/mainbbs.php` へ統一した | `fixtures/main-list.html`、`fixtures/main-article.html` | ブラウザ回帰 T-09、T-10 OK |
| R-03 | P0 | 完了 | 感想フィクスチャの記事IDパラメータ修正 | 感想ページリンクを `no=10001` へ修正した | `fixtures/impression.html` | ブラウザ回帰 T-08、T-13 OK |
| R-04 | P0 | 完了 | 投稿者情報の個人初期値を除去 | CONFIGと設定画面定義の名前、トリップ、パスワードを空文字へ変更した。保存済み設定の読込処理は変更していない | `Arcadia_toolbar_next.js` | 個人初期値の残存なし。保存済み検証値によるT-08、T-10、T-12、T-13 OK |
| R-05 | P0 | 完了 | 回帰確認手順の確立 | 依存パッケージ不要のブラウザランナーと実行手順を追加した | 本台帳、`tests/browser-runner.html`、`tests/README.md` | T-01～T-28を同一画面で実行・集計可能。2026-09-16に28/28 OK |
| R-06 | P1 | 完了 | バージョン表記の統一 | 起動ログを `v5.01` へ修正し、「完成版」「修正完了版」の表現を除去した | `Arcadia_toolbar_next.js` | メタデータと起動ログが `5.01` で一致 |
| R-07 | P1 | 完了 | localStorageアクセスの完全集約 | `hasThemePreference()` をStorageManagerへ追加し、ThemeManagerの直接参照を置換した | `Arcadia_toolbar_next.js` の `StorageManager`、`ThemeManager` | `localStorage` の実アクセスがStorageManager内だけに存在することを静的確認済み |
| R-08 | P1 | 完了 | お気に入り更新イベントの重複通知整理 | StorageManager発の旧互換イベントへ識別情報を付け、EventBusでの二重中継を抑止した | `StorageManager`、`EventBus`、お気に入り更新購読箇所 | 旧イベント発火は維持し、新イベントと旧イベントの両方を内部処理しない構造へ変更。回帰28/28 OK |
| R-09 | P1 | 完了 | ビルド不要のフィクスチャ検証方法を用意 | ローカルHTTP上のiframeへ各routeを設定し、現行Userscriptを注入するブラウザランナーを追加した | `tests/browser-runner.html`、`tests/README.md` | npm・build・配布コードのテスト専用分岐なしで、2026-09-16に28/28 OK |
| R-10 | P2 | 完了 | `innerHTML` 依存の段階的削減 | IndexPopupHandlerをDOM APIへ置換。StyleControlBarも原文をDocumentFragmentで保持し、5種類の整形をテキストノードと要素操作へ置換した | `IndexPopupHandler`、`StyleControlBar` | `Arcadia_toolbar_next.js` の `innerHTML` 残存なし。整形専用T-18～T-25を含む回帰28/28 OK |
| R-11 | P2 | 完了 | 非blocking通知への移行 | 設定保存・リセットの `alert()` を廃止し、`role=status` と `aria-live` を持つ画面内通知へ変更した | `SettingsEditor` | `alert()` の残存なし。T-16を含む全回帰28/28 OK |
| R-12 | P2 | 完了 | コピー処理の互換性改善 | Clipboard APIを優先し、失敗時は `execCommand`、さらに失敗時は選択済み内容の手動コピー案内へフォールバックする | `FavoritesManager` | 成否を `role=status` で通知し、Clipboard APIが使えない環境でも内容を失わない |
| R-13 | P2 | 完了 | READMEの実用文書化 | 導入、機能、保存キー、プライバシー、開発用ファイル、制約を追記した | `README.md` | 現行版の導入・更新・バックアップ・検証方法をREADMEから辿れる |
| R-14 | P2 | 完了 | 現行版と旧版の配置整理 | 物理移動は行わず、READMEで現行配布対象と旧版参照用を明示した | ルート構成、README | 現行版だけを有効化することと、旧版との同時有効化禁止を明記済み |
| R-15 | P3 | 見送り | 大規模リファクタリングの実施判断 | 28件の回帰を確立し、主要なHTML文字列依存を段階改修で解消できた。Parser、Renderer、Matcher、Featureの責務分離も機能しており、現時点で全面書き直しを必要とする複数の具体的問題は残っていない | `Arcadia_toolbar_next.js` 全体 | 単一Userscript・npmなし・buildなしを維持する。修正困難な問題が複数顕在化した場合だけ再評価する |
| R-16 | P1 | 完了 | 保存設定の型検証と再帰補完 | 従来はJSONとして有効なら既知設定の異常型も実行時へ混入し、入れ子の部分設定は浅いマージだった。既定値をスキーマにして既知キーを型検証し、入れ子を再帰補完する。未知キーは将来互換のため保持する | `StorageManager.getConfig()` | 破損JSONと異常型の回帰T-26、T-27を含む28/28 OK |
| R-17 | P1 | 完了 | テーマ更新イベントの重複通知整理 | 手動切替時はStorageManagerのwindowイベント中継とThemeManagerの直接emitで内部通知が2回発生していた。DOM適用を先行し、通知をStorageManager経由へ一本化した | `ThemeManager`、`StorageManager`、`EventBus` | T-28で1クリックにつき内部通知1回、DOM・保存値更新を確認。回帰28/28 OK |

## 画面別回帰確認

結果欄は `未確認`、`OK`、`NG`、`対象外` のいずれかを記入する。NGの場合は関連する改善IDまたは新しいIDを付ける。

| ID | フィクスチャ | 確認内容 | 結果 | 確認環境・証跡 |
| --- | --- | --- | --- | --- |
| T-01 | `ss-list.html` | 通常SS一覧の再構築、全話・感想リンク、お気に入り色、NG非表示 | OK | Chromium系ブラウザ、ローカルHTTP、`tests/browser-runner.html`、2026-09-16 |
| T-02 | `ss-list-18.html` | 18禁一覧のtable位置、一覧再構築、警告スキップ用リンク処理 | OK | 同上 |
| T-03 | `ss-list-chiraura.html` | チラシの裏固有の列構造、全話リンク、お気に入り色、NG非表示 | OK | 同上 |
| T-04 | `ss-article-first.html` | 第1話で次話リンクと目次を正しく扱う | OK | 同上 |
| T-05 | `ss-article-middle.html` | 中間話で前話・次話リンクと歯抜け探索が正しく動く | OK | 同上 |
| T-06 | `ss-article-last.html` | 最終話で前話リンクと目次を正しく扱う | OK | 同上 |
| T-07 | `ss-article-all.html` | 全話表示の目次生成、アンカー、体裁変更が正しく動く | OK | 同上 |
| T-08 | `impression.html` | 感想逆順、日本語日付、ページリンク、投稿フォーム補完が正しく動く | OK | 同上 |
| T-09 | `main-list.html` | メイン掲示板一覧の再構築、お気に入り背景色、NG非表示 | OK | 同上 |
| T-10 | `main-article.html` | 返信フォームだけが自動入力対象となり、記事本文を変更しない | OK | 同上 |
| T-11 | `search-list.html` | 捜索掲示板一覧の再構築、お気に入り背景色、NG非表示 | OK | 同上 |
| T-12 | `search-article.html` | 返信フォームだけが自動入力対象となり、記事本文を変更しない | OK | 同上 |
| T-13 | `impression.html` | 編集・削除フォームには名前、トリップ、パスワードを自動入力しない | OK | 同上 |
| T-14 | 一覧4種 | お気に入り・NGが完全一致ではなくタイトルへの部分一致で動作する | OK | T-01～T-03、T-09、T-11で合成部分一致語を使用 |
| T-15 | 全画面 | pageshow後に二重初期化せず、コンソール例外がない | OK | 全13画面の例外捕捉とtheme-toggle重複数を確認 |
| T-16 | `ss-list.html` | 設定保存がalertを表示せず、画面内の状態通知へ反映される | OK | Clipboard等を使わない設定保存操作をブラウザランナーで実行 |
| T-17 | `ss-list.html` | お気に入り書き出しがClipboard APIを優先し、成功を画面内通知する | OK | テスト内のClipboard APIスタブで合成データの受渡しを確認。実クリップボードは変更しない |
| T-18 | `ss-article-formatting.html` | 空行整形が3連続以上の改行を2つへ圧縮する | OK | Chromium系ブラウザ、ローカルHTTP、`tests/browser-runner.html`、2026-09-16 |
| T-19 | `ss-article-formatting.html` | 行頭整形が通常段落の先頭へ全角空白を挿入する | OK | 同上 |
| T-20 | `ss-article-formatting.html` | 改行整形が文中の不要な改行を除去する | OK | 同上 |
| T-21 | `ss-article-formatting.html` | 連字整形が7文字の連続箇所へ `wbr` を挿入する | OK | 同上 |
| T-22 | `ss-article-formatting.html` | 挿行整形が地の文と会話の境界へ空行を挿入する | OK | 同上 |
| T-23 | `ss-article-formatting.html` | 全整形を解除すると初期DOMへ完全復元する | OK | 同上 |
| T-24 | `ss-article-formatting.html` | 空行圧縮と改行除去の併用時も2改行を維持する | OK | 同上 |
| T-25 | `ss-article-formatting.html` | 連字整形が属性値や既存の装飾要素を変更しない | OK | 同上 |
| T-26 | `ss-list.html` | 破損した設定・お気に入りJSONから既定値で安全に起動する | OK | Chromium系ブラウザ、ローカルHTTP、`tests/browser-runner.html`、2026-09-16 |
| T-27 | `ss-article-first.html` | 既知設定の異常型だけを既定値へ戻し、正しい型の保存値を維持する | OK | 同上 |
| T-28 | `ss-list.html` | テーマ切替1回につき内部テーマ更新通知を1回だけ発火する | OK | 内部EventBus購読プローブで修正前2回、修正後1回を確認。DOMと保存値の更新も確認 |

## 推奨実施順

1. R-01～R-04を小さな修正コミットとして実施する。
2. R-05とR-09で、合成HTMLを使った確認方法を確立する。
3. T-01～T-15を実行し、結果と証跡を本台帳へ記録する。
4. R-06～R-08の整合性・イベント処理を修正し、影響するテストを再実行する。
5. R-10～R-13を1項目ずつ実装し、項目ごとにコミットする。
6. R-14とR-15は、回帰確認結果を根拠として方針を決める。

## 大規模リファクタリングの判断基準

次の条件をすべて満たすまでは、全面書き直しを開始しない。

- T-01～T-15の基準挙動が確定している。
- 現行コードで修正困難な具体的問題が複数残っている。
- 単体Userscript、npmなし、buildなしという配布条件を維持できる。
- お気に入り・NGの部分一致、table再構築順、イベント順を維持する検証方法がある。
- 旧版と新版を同時実行せず、機能単位で切り替えられる。

条件を満たした場合も、最初に検討するのは全面置換ではなく、次の段階改修とする。

1. URL・DOM依存をArcadiaDOMParserとRouteManagerへ集約する。
2. 文字列HTML加工をRendererのDOM操作へ置き換える。
3. Featureを初期化とイベント接続に限定する。
4. 共有イベントの登録・解除と重複通知を整理する。
5. 最後に起動処理をページ種別ごとの宣言的な登録方式へ整理する。

### 2026-09-16 再判定

- URL・DOM解析、一覧描画、照合、画面機能は既にクラス単位で分離されている。
- `IndexPopupHandler` と `StyleControlBar` のHTML文字列加工はDOM操作へ移行できた。
- 主要13画面、体裁整形8ケース、保存データ耐障害性2ケース、テーマ通知1ケースを含む28件のブラウザ回帰が通過した。
- 全面書き直しでしか解決できない不具合や責務衝突は確認されていない。

以上から、現時点では全面書き直しを見送る。以後も不具合単位・責務単位の段階改修を基本とし、R-15の完了条件に記載した状況が生じた場合に再評価する。

## 更新履歴

| 日付 | 変更 | 関連コミット |
| --- | --- | --- |
| 2026-09-16 | 初版作成。改善候補15件と画面別回帰確認15件を登録 | `05bf48c` |
| 2026-09-16 | R-01～R-09を実装。ブラウザ回帰T-01～T-15が15/15 OK | `05bf48c` |
| 2026-09-16 | R-11～R-14を完了。R-10のIndexPopupHandlerをDOM API化し、T-16・T-17追加後の再回帰17/17 OK | `05bf48c` |
| 2026-09-16 | R-10を完了。StyleControlBarをDOM操作へ移行し、T-18～T-25追加後の回帰25/25 OK。R-15は段階改修継続として見送り | `fb96eac` |
| 2026-09-16 | R-16を完了。設定の再帰型検証とT-26・T-27を追加し、回帰27/27 OK | `e90ee95` |
| 2026-09-16 | R-17を完了。テーマ更新通知を一本化し、T-28追加後の回帰28/28 OK | 未コミット |
