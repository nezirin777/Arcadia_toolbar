# ArcadiaToolBar Next 段階改修台帳

## 目的

既存の動作と単体Userscript配布を維持したまま、繰り返し不具合が発生した責務境界を順番に整理する。

本台帳は、完了済みの不具合・改善項目を記録する `改善作業台帳.md` とは分離する。構造改修中に見つけた機械的な作業を新しい行として増やさず、各段階の範囲内で扱う。

## 固定条件

- 配布物は `Arcadia_toolbar_next.js` 1ファイルのままとする。
- npm、build、ESModulesを導入しない。
- localStorageのキーと既存データ形式を維持する。
- URL、DOMクラス・ID、内部イベント名、お気に入り入出力形式を、明示した移行なしに変更しない。
- 旧実装と新実装を同時に動かさず、段階ごとに置換する。
- 各段階は独立して回帰確認できる状態で終える。
- 既存58件と、その段階で必要な追加回帰がすべて通るまで次段階へ進まない。
- 各段階を原則1コミットとし、段階の途中で別段階へ着手しない。

## 状態

| 状態 | 意味 |
| --- | --- |
| 未着手 | 前段階の完了後に開始する |
| 対応中 | 実装または検証中 |
| 完了 | 実装、静的確認、ブラウザ回帰が完了した |
| 保留 | 必須段階ではなく、再評価条件を満たすまで実施しない |
| 中止 | 互換性を安全に維持できず、変更を元に戻した |

## 改修段階

| ID | 状態 | 段階 | 対象 | 完了条件 |
| --- | --- | --- | --- | --- |
| SR-00 | 完了 | 回帰基準の固定 | `tests/browser-runner.html`、`改善作業台帳.md` | 構造改修前のブラウザ回帰58/58 OK、ワークツリーの基準コミット `ad3d2b8` を確認 |
| SR-01 | 完了 | 設定スキーマの一本化 | `CONFIG`、`ConfigManager`、`SettingsEditor` | 編集項目の既定値・型・表示情報を単一定義から生成し、保存互換を維持。設定項目網羅テストを追加して全回帰OK |
| SR-02 | 完了 | お気に入り変換処理の分離 | `FavoritesManager`、新規 `FavoritesCodec` | 入出力・エスケープ・複数行メモ解析を副作用のない変換責務へ移し、既存形式の往復結果を維持 |
| SR-03 | 未着手 | 一覧更新パイプラインの一本化 | `ListFormatter`、`SpamFilter`、Parser/Renderer | table再構築、判定、装飾、スパム除外の順序とParser所有者を明示し、古い非同期処理による上書きを防止 |
| SR-04 | 未着手 | 起動と機能ライフサイクルの整理 | `boot()`、Featureのイベント登録 | ページ種別・設定条件・初期化を宣言的に登録し、イベント登録と解除の所有者を明示 |
| SR-05 | 保留 | 体裁バーの内部責務分離 | `StyleControlBar` | SR-01～SR-04完了後も修正困難な責務衝突が残る場合だけ、変換・状態・UI制御を内部分離 |

## 段階別の非変更範囲

### SR-01

- `arcadia_config` のキー名と保存オブジェクト構造を変更しない。
- 未知キーを保持する読込互換を維持する。
- 設定画面の入力ID、表示順、ラベル、入力型、既定値を変更しない。

### SR-02

- 書き出しテキストの見出し、区切り、エスケープ表現を変更しない。
- 空・未認識インポート時に既存データを保護する。
- 同一カテゴリ内の重複統合規則を変更しない。

### SR-03

- お気に入り・NGの部分一致規則と背景色クラスを変更しない。
- 元tableの列解釈、行順、直リンク、PV判定の結果を変更しない。
- `requestAnimationFrame` の利用自体を目的なく同期処理へ戻さない。

### SR-04

- 対象ページと機能の有効条件を変更しない。
- bfcache復帰時の二重初期化防止を維持する。
- Feature間で直接参照を追加しない。

## 中止・再評価条件

次のいずれかが発生した段階は変更を元に戻し、全面書き直しへ自動移行せず再評価する。

- 保存済み設定またはお気に入りを互換形式のまま読み込めない。
- 実サイトのDOMにテストフィクスチャだけでは表現できない破壊的な差異が見つかった。
- 既存回帰の期待値変更が必要になり、構造改修だけではなく仕様変更を伴う。
- 単体Userscript、npmなし、buildなしの条件を維持できない。

## 完了条件

必須範囲はSR-01～SR-04とする。全段階完了後に通常監査へ戻し、SR-05や全面書き直しへは自動的に進まない。

## 作業記録

| 日付 | 段階 | 内容 | 検証 | コミット |
| --- | --- | --- | --- | --- |
| 2026-09-17 | SR-00 | 58件の合成ブラウザ回帰を構造改修前基準として固定 | 58/58 OK | `ad3d2b8` |
| 2026-09-17 | SR-01 | `CONFIG_SCHEMA`を単一定義とし、実行時既定値と設定画面を生成。T-59で全37項目の表示値・保存値を固定 | `node --check`、`git diff --check`、ブラウザ回帰59/59 OK | `59a6b25` |
| 2026-09-17 | SR-02 | `FavoritesCodec`へテキスト生成・解析・タイトルのエスケープを分離。カテゴリ表示定義をUIとCodecで共有し、Managerは画面・保存の調整に限定 | `node --check`、`git diff --check`、T-60を含むブラウザ回帰60/60 OK | 未コミット |
